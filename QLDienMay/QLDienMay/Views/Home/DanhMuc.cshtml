@model IEnumerable<QLDienMay.Models.DANHMUC>

@foreach (var item in Model)
{
    <li class="dropdown">
        @*<a href="#" class="dropdown-toggle" data-toggle="dropdown">Tủ lạnh - Tủ đông</a>*@
        <a href="/Home/DanhMuc/@item.MADANHMUC">@item.TENDANHMUC</a>
        <div class="dropdown-menu">
            <div class="dropdown-inner">
                <ul class="list-unstyled">
                    @Html.Action("Loai", "Home", new { maDm = item.MADANHMUC })
                </ul>
            </div>
        </div>
    </li>
}
